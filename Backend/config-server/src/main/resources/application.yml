server:
  port: 8888
  address: 127.0.0.1

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
  profiles:
    active: native

management:
  endpoints:
    web:
      exposure:
        include: health,info