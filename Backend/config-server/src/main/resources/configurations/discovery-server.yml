#Backend/config-server/src/main/resources/configurations/discovery-server.yml
server:
  port: 8761
  # PUBLIC - Dashboard için

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0
    enable-self-preservation: true

management:
  endpoints:
    web:
      exposure:
        include: health,info